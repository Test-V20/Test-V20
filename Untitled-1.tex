\documentclass{article}
\usepackage[margin=1in]{geometry}
\usepackage{tikz}
\usetikzlibrary{mindmap}

\begin{document}

\begin{tikzpicture}[mindmap,
    grow cyclic,
    every node/.style={concept, execute at begin node=\hskip0pt},
    root concept/.append style={concept color=blue!50, font=\Large\bfseries},
    level 1/.append style={level distance=5cm, sibling angle=60, concept color=blue!40},
    level 2/.append style={level distance=3cm, sibling angle=45, concept color=blue!30}]

  \node[root concept] {Nvidia's Meteoric Rise}
    child { node {Humble Beginnings}
      child { node {Founded in 1993 by Jensen Huang} }
      child { node {Huang: Former Denny's waiter} }
    }
    child { node {GPU Pioneer}
      child { node {Launched GeForce 256} }
      child { node {Revolutionized graphics} }
    }
    child { node {Strategic Shift to AI}
      child { node {Released CUDA in 2006} }
      child { node {AI ecosystem integration} }
    }
    child { node {CUDA Platform}
      child { node {GPU power for AI} }
      child { node {Default for AI developers} }
    }
    child { node {Market Dominance}
      child { node {Most valuable in 2024} }
      child { node {Wide digital applications} }
    }
    child { node {10:1 Stock Split}
      child { node {More affordable shares} }
      child { node {Growing AI investor demand} }
    };
\end{tikzpicture}
\end{document}
